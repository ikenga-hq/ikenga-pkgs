{
  "name": "@ikenga/pkg-browser",
  "version": "0.1.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ikenga-hq/ikenga-pkgs.git",
    "directory": "packages/sidecars/playwright-browser"
  },
  "private": false,
  "description": "Playwright verb engine behind Ikenga's /iyke/browser/* contract \u2014 replaces the Rust chromiumoxide Chrome engine (ADR-018). Managed (dedicated profile) + Attach (everyday Chrome via the official Playwright extension).",
  "license": "Apache-2.0",
  "type": "module",
  "files": [
    "dist",
    "node_modules",
    "manifest.json",
    "README.md"
  ],
  "scripts": {
    "build": "bun build src/sidecar.ts --target node --outfile dist/sidecar.js --external playwright --external playwright-core",
    "test": "node --test --import=tsx 'src/**/*.test.ts'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "publish:flat": "bash scripts/publish-flat.sh"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  },
  "bundledDependencies": [
    "playwright"
  ],
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^22.10.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.2"
  }
}
