{
  "name": "@ikenga/pkg-meetings-bot",
  "version": "0.1.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ikenga-hq/ikenga-pkgs.git",
    "directory": "packages/sidecars/meetings-bot"
  },
  "description": "Ikenga meeting recorder sidecar managing local own-machine capture, whisper.cpp STT engine, and Playwright bot runners.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/sidecar.js",
  "types": "dist/sidecar.d.ts",
  "files": [
    "dist",
    "manifest.json",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"const f='dist/sidecar.js',fs=require('fs');const s=fs.readFileSync(f,'utf8');if(!s.startsWith('#!'))fs.writeFileSync(f,'#!/usr/bin/env node\\n'+s);fs.chmodSync(f,0o755)\"",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist",
    "test": "node --test --import=tsx src/capture/preflight.test.ts src/capture/ffmpeg-graph.test.ts src/capture/channel-extract.test.ts src/whisper/models.test.ts src/whisper/binary.test.ts src/whisper/engine.test.ts src/sidecar.test.ts src/cli.test.ts src/session.test.ts src/adapters/google-meet.test.ts src/auth/challenge-detect.test.ts src/whisper/acquire.test.ts src/calendar/ics.test.ts"
  },
  "dependencies": {
    "@ikenga/meetings-contract": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.4.0"
  }
}
